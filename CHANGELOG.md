# Changelog

**2026-08-27**: Unified API and renderer readiness checks under the single `/ready` route and aligned local debugging, tests, deployment, and architecture documentation (`backend/app/api/health.py`, `backend/app/renderer_main.py`, `bin/debug.sh`, `backend/tests/test_v2_app.py`, `backend/AGENTS.md`, `doc/REFACTOR.md`, `doc/GCP.md`, `CHANGELOG.md`).

**2026-08-27**: Reduced API cold-start imports by lazy-loading optional LLM, storage, and PDF dependencies; moved production schema initialization to a Cloud Run migration Job (with explicit SQLModel registration); and added a 3-minute Cloud Scheduler `/ready` keep-warm job with shutdown cleanup (`backend/app/api/health.py`, `backend/app/integrations/llm/factory.py`, `backend/app/integrations/storage/factory.py`, `backend/app/lessons/sources.py`, `backend/scripts/init_db.py`, `bin/prepare.sh`, `bin/deploy.sh`, `doc/GCP.md`, `doc/COST.md`).

**2026-08-26**: Added PNG, JPEG, and WebP lesson sources alongside PDFs, including drag-and-drop selection, image attachment indicators, binary signature and shared-size validation, private source storage with correct media types, multimodal Vertex Gemini/OpenAI requests, early rejection for text-only providers, and end-to-end/provider regression coverage (`backend/app/api/generations.py`, `backend/app/integrations/llm/base.py`, `backend/app/integrations/llm/deepseek.py`, `backend/app/integrations/llm/gemini.py`, `backend/app/integrations/llm/openai.py`, `backend/app/lessons/generation.py`, `backend/app/lessons/sources.py`, `backend/tests/test_v2_api.py`, `backend/tests/test_v2_app.py`, `frontend/src/components/ui/Textarea.tsx`, `frontend/src/lib/hooks/useGeneration.ts`, `README.md`, `backend/AGENTS.md`, `doc/REFACTOR.md`, `CHANGELOG.md`).

**2026-08-26**: Restyled the Move lesson folder picker's scrollbar with a thin dark thumb and transparent track so it matches the modal's chalkboard theme (`frontend/src/components/dashboard/FolderPicker.tsx`, `CHANGELOG.md`).

**2026-08-26**: Extended the Dashboard Lessons row background through the add-folder action so the active navigation highlight forms one continuous container (`frontend/src/components/dashboard/DashboardSidebar.tsx`, `CHANGELOG.md`).

**2026-08-26**: Added persistent owner-scoped lesson folder trees with nested creation, rename, leaf-only deletion and automatic lesson promotion to the parent; moved Search above Lessons in the Dashboard sidebar, added URL-persistent folder navigation and course movement controls, extended additive database migration and API schemas, and covered hierarchy rules and tenant isolation with regression tests (`backend/app/api/folders.py`, `backend/app/api/lessons.py`, `backend/app/api/schemas.py`, `backend/app/db/folders.py`, `backend/app/db/lessons.py`, `backend/app/db/models.py`, `backend/app/db/session.py`, `backend/app/main.py`, `backend/tests/test_v2_api.py`, `backend/AGENTS.md`, `frontend/src/app/dashboard/layout.tsx`, `frontend/src/app/dashboard/page.tsx`, `frontend/src/app/dashboard/search/page.tsx`, `frontend/src/components/dashboard/DashboardSidebar.tsx`, `frontend/src/components/dashboard/FolderPicker.tsx`, `frontend/src/components/dashboard/LessonCard.tsx`, `frontend/src/components/dashboard/LessonFoldersProvider.tsx`, `frontend/src/components/dashboard/LessonGrid.tsx`, `frontend/src/lib/api/folders.ts`, `frontend/src/lib/api/lessons.ts`, `frontend/src/lib/hooks/useLessons.ts`, `frontend/src/lib/types/api.ts`, `CHANGELOG.md`).

**2026-08-26**: Linked the Dashboard sidebar logo and Chalksmith.ai wordmark to the public `https://chalksmith.ai/` homepage so the app-host brand link no longer redirects back to `/home` (`frontend/src/components/dashboard/DashboardSidebar.tsx`, `CHANGELOG.md`).

**2026-08-26**: Fixed the Clerk avatar menu's Public profile entry to use the installed SDK's compound menu-item API, ensuring the link is rendered with an icon (`frontend/src/components/auth/AuthButton.tsx`, `CHANGELOG.md`).

**2026-08-26**: Added a visible Public profile entry to the Clerk avatar menu so users can find and edit their public introduction without losing account settings access (`frontend/src/components/auth/AuthButton.tsx`, `CHANGELOG.md`).

**2026-08-26**: Restored Clerk account settings access from the avatar menu and added an Account settings action to the custom public profile editor, preserving password and security controls (`frontend/src/components/auth/AuthButton.tsx`, `frontend/src/components/profile/ProfileEditor.tsx`, `CHANGELOG.md`).

**2026-08-26**: Clarified that only the original author can continue editing published lessons and removed the content-page header divider and extra padding to reduce space before search (`frontend/src/app/content/page.tsx`, `CHANGELOG.md`).

**2026-08-26**: Placed the Dashboard published indicator immediately to the right of each lesson date in the card footer (`frontend/src/components/dashboard/LessonCard.tsx`, `CHANGELOG.md`).

**2026-08-26**: Moved the published indicator to the Dashboard card footer immediately before the lesson date for consistent alignment away from variable-length titles (`frontend/src/components/dashboard/LessonCard.tsx`, `CHANGELOG.md`).

**2026-08-26**: Fixed the Dashboard published icon position by anchoring it beside the card actions instead of after the variable-length lesson title (`frontend/src/components/dashboard/LessonCard.tsx`, `CHANGELOG.md`).

**2026-08-26**: Added published-state metadata to Dashboard lesson summaries and a compact Explore globe icon for published cards; removed the owner byline from the lesson detail header (`backend/app/db/lessons.py`, `backend/app/api/schemas.py`, `backend/app/api/lessons.py`, `frontend/src/lib/types/api.ts`, `frontend/src/components/dashboard/LessonGrid.tsx`, `frontend/src/components/dashboard/LessonCard.tsx`, `frontend/src/app/generation/page.tsx`, `CHANGELOG.md`).

**2026-08-26**: Replaced published lesson format labels with author bylines directly beneath each lesson title on the content page (`frontend/src/components/content/PublishedLessonGrid.tsx`).

Project history, newest first. One entry per completed coding or design task:
what changed, why, and the files affected. Agent instructions live in
[AGENTS.md](AGENTS.md).

Format: `**YYYY-MM-DD**: [Brief description of changes with which files were edited]`

- **2026-08-26**: Added owner-controlled lesson publishing with a confirmation dialog and reversible Unpublish action; exposed only the published root's ready final revision through unauthenticated Explore list/view/download endpoints; added public lesson cards, a non-functional search placeholder, and the preserved YouTube channel link; migrated and indexed publication timestamps and added API regression coverage (`backend/app/api/explore.py`, `backend/app/api/lesson_access.py`, `backend/app/api/lessons.py`, `backend/app/api/schemas.py`, `backend/app/db/lessons.py`, `backend/app/db/models.py`, `backend/app/db/session.py`, `backend/app/main.py`, `backend/scripts/sync_lessons.py`, `backend/tests/test_v2_api.py`, `backend/tests/test_v2_app.py`, `backend/AGENTS.md`, `frontend/src/app/content/page.tsx`, `frontend/src/app/generation/page.tsx`, `frontend/src/components/content/PublishedLessonGrid.tsx`, `frontend/src/components/generation/PublishLessonButton.tsx`, `frontend/src/lib/api/client.ts`, `frontend/src/lib/api/explore.ts`, `frontend/src/lib/api/lessons.ts`, `frontend/src/lib/hooks/useGeneration.ts`, `frontend/src/lib/types/api.ts`, `CHANGELOG.md`).
- **2026-08-26**: Unified Manim LaTeX sizing by giving standalone and derivation equations the same base font size, reserving compact math for diagram annotations, and rejecting expressions that would require more than ten percent downscaling so the repair pass splits long chains into consistently sized formulas (`backend/app/lessons/formats/video/compiler.py`, `backend/app/lessons/formats/video/prompt.py`, `backend/tests/test_v2_app.py`, `doc/LAYOUT.md`, `CHANGELOG.md`).
- **2026-08-26**: Standardized code-driven Manim videos with a compiler-injected Chalksmith runtime that owns the chalkboard palette, Inter/Noto CJK typography roles, background, text fitting, and `MathTex` rendering; required generated scenes to use `cs_text`/`cs_math`, rejected direct text objects and style overrides with the existing repair path, stripped prior runtimes during lesson edits, added renderer fonts and a LaTeX/`dvisvgm` toolchain, persisted video runtime/compiler versions, and added prompt, compiler, security, edit, and AST regression coverage (`backend/app/lessons/formats/video/compiler.py`, `backend/app/lessons/formats/video/prompt.py`, `backend/app/lessons/formats/video/strategy.py`, `backend/tests/test_v2_app.py`, `docker/renderer.Dockerfile`, `backend/AGENTS.md`, `README.md`, `doc/LAYOUT.md`, `CHANGELOG.md`).
- **2026-08-26**: Added compact format icons beside lesson-list card titles so Interactive, Presentation, and Video lessons can be distinguished at a glance while retaining the existing text format label for accessibility (`frontend/src/components/dashboard/LessonFormatIcon.tsx`, `frontend/src/components/dashboard/LessonCard.tsx`, `CHANGELOG.md`).
- **2026-08-26**: Required Interactive lessons to render learner-visible notation with approved KaTeX assets instead of exposing raw LaTeX delimiters, re-typeset dynamically changed formulas without doing so every frame, and perform a pre-output state/handler review that keeps every tab's HTML and p5 canvas synchronized while detecting missing functions or DOM ids, invalid p5 calls, API-name shadowing, exceptions, and frozen draw loops (`backend/app/lessons/formats/interactive/prompt.py`, `backend/tests/test_v2_api.py`, `CHANGELOG.md`).
- **2026-08-26**: Reorganized the Interactive generation Prompt into deliverable, teaching-and-interaction, visual-layout, pointer-coordinate, runtime-safety, and security sections; required generated text, lines, plots, controls, and feedback to avoid unintended overlap and use balanced spacing and visual density, while preserving the existing p5.js, accuracy, responsiveness, accessibility, loop-termination, and security constraints (`backend/app/lessons/formats/interactive/prompt.py`, `backend/tests/test_v2_api.py`, `CHANGELOG.md`).
- **2026-08-26**: Reorganized code-driven lesson generation and repair Prompts into explicit priority, untrusted-context, output-contract, lesson-requirement, format-rule, and repair-task sections; clarified advanced-topic fidelity and edit preservation while retaining all existing teaching, source-handling, safety, and response constraints, with regression coverage for the preserved guidance and dynamic context (`backend/app/lessons/formats/code.py`, `backend/tests/test_v2_app.py`, `backend/tests/test_v2_api.py`, `CHANGELOG.md`).
- **2026-08-26**: Made Slides generation leave a safe length margin for custom HTML during initial generation and its single repair pass, preventing near-limit retries such as Version 3's 6262-character block from failing the 6000-character validator; exposed public per-version errors in lesson history and rendered failures once as red conversation messages instead of duplicating them above the generation controls (`backend/app/lessons/formats/slides/blocks/custom.py`, `backend/app/lessons/formats/slides/response.py`, `backend/app/db/lessons.py`, `backend/app/api/schemas.py`, `backend/tests/test_v2_app.py`, `backend/tests/test_v2_api.py`, `frontend/src/lib/types/api.ts`, `frontend/src/lib/hooks/useGeneration.ts`, `frontend/src/components/generation/GenerationSidebar.tsx`, `frontend/src/app/generation/page.tsx`, `CHANGELOG.md`).
- **2026-08-26**: Streamlined the v2 API tests without removing independent regression coverage: reused SSE parsing, replaced repeated dynamic imports, centralized database-connection tracking, removed compiler-level Slides assertions already owned by app tests, and dropped duplicated format/version branches (`backend/tests/test_v2_api.py`, `CHANGELOG.md`).
- **2026-08-26**: Required Slides lessons to use LaTeX-formatted equation Blocks for mathematical formulas and to keep each geometry diagram with its corresponding explanation on the same slide, preserving compatible notation rendering and coherent visual instruction (`backend/app/lessons/formats/slides/prompt.py`, `backend/tests/test_v2_app.py`, `CHANGELOG.md`).
- **2026-08-26**: Enabled PDF attachment selection before a topic or format is entered and allowed generation from an uploaded PDF without typed text; source-only lessons use the PDF filename as their initial title, while source-only revisions receive an explicit default edit instruction (`frontend/src/components/generation/InputForm.tsx`, `frontend/src/lib/hooks/useGeneration.ts`, `CHANGELOG.md`).
- **2026-08-25**: Reduced Dashboard lesson-list loading from a full Lesson-row fetch to a lightweight card projection, avoiding transfer of source code, specifications, and private diagnostics over the local Cloud SQL Proxy while preserving the batched version-count query (`backend/app/db/lessons.py`, `backend/tests/test_v2_api.py`, `CHANGELOG.md`).
- **2026-08-25**: Merged the Slides Prompt formatting changes so dynamic lesson context remains visually separated while the generated Block Catalog keeps explicit multiline boundaries (`backend/app/lessons/formats/slides/prompt.py`, `CHANGELOG.md`).
- **2026-08-23**: Increased hierarchy branch headings and child-list typography, spacing, and left padding so generated branch cards match the lesson reference layout without changing flow-diagram sizing (`backend/app/lessons/formats/slides/assets/v1/blocks/diagrams.css`, `CHANGELOG.md`).
- **2026-08-18**: Reorganized the structured Slides generation Prompt into explicit priority, untrusted-context, output-contract, lesson-requirement, Block-selection, geometry, composition, layout-ownership, custom-html, and Schema sections; consolidated repeated semantic-Block, layout, and custom-html guidance while preserving every existing safety, validation, teaching, and visual-planning constraint, and added regression coverage for section boundaries and nonduplicated dynamic-context instructions (`backend/app/lessons/formats/slides/prompt.py`, `backend/tests/test_v2_app.py`, `backend/tests/test_v2_api.py`, `CHANGELOG.md`).
- **2026-08-18**: Clarified the structured Slides boundaries by moving model-response extraction, bounded JSON recovery, Spec normalization, validation diagnostics, and repair Prompt construction from the strategy adapter into `response.py`; colocated the complete custom-html contract, Prompt policy, HTML/CSS/SVG sanitizer, capacity helpers, scope resolution, and renderer in the custom Block vertical slice; removed the obsolete root-level sanitizer module and updated imports, tests, and architecture guidance without changing behavior (`backend/app/lessons/formats/slides/response.py`, `backend/app/lessons/formats/slides/strategy.py`, `backend/app/lessons/formats/slides/blocks/custom.py`, `backend/app/lessons/formats/slides/sanitizer.py`, `backend/app/lessons/formats/slides/prompt.py`, `backend/tests/test_v2_app.py`, `backend/AGENTS.md`, `doc/LAYOUT.md`, `CHANGELOG.md`).
- **2026-08-18**: Corrected local Slides JSON recovery around quoted prose and nested discriminated Blocks while retaining one bounded model-repair fallback; separated rejected model output and first-error diagnostics from public lesson data; injected application environment into generation error sanitization; enforced same-format revisions and unique serialized version numbers; and added an explicit ready-version final selection API, dashboard projection, and clickable star beside each Version label (`backend/app/lessons/formats/slides/strategy.py`, `backend/app/lessons/generation.py`, `backend/app/db/models.py`, `backend/app/db/session.py`, `backend/app/db/lessons.py`, `backend/app/api/generations.py`, `backend/app/api/lessons.py`, `backend/app/api/schemas.py`, `backend/scripts/sync_lessons.py`, `backend/tests/test_v2_app.py`, `backend/tests/test_v2_api.py`, `frontend/src/app/generation/page.tsx`, `frontend/src/components/generation/GenerationSidebar.tsx`, `frontend/src/lib/api/lessons.ts`, `frontend/src/lib/hooks/useGeneration.ts`, `frontend/src/lib/types/api.ts`, `backend/AGENTS.md`, `doc/REFACTOR.md`, `CHANGELOG.md`).
- **2026-08-18**: Counted only learner-visible on-slide text toward slide capacity—decoded HTML entities, excluding structural keys and accessibility descriptions—and raised the per-slide budget from 520 to 640 characters (`backend/app/lessons/formats/slides/spec.py`, `backend/app/lessons/formats/slides/blocks/custom.py`, `backend/app/lessons/formats/slides/sanitizer.py`, `backend/app/lessons/formats/slides/prompt.py`, `backend/tests/test_v2_app.py`, `CHANGELOG.md`).
- **2026-08-18**: Added a specific shared reset for unordered and ordered lists inside Slides cards, preventing Reveal.js from reintroducing its `1em`/40px left margin while leaving each Block's padding, marker, and gap rules intact (`backend/app/lessons/formats/slides/assets/v1/core.css`, `backend/tests/test_v2_app.py`, `.env/storage/lessons/user_3G5KeDTZSDI5ogr34JnDy27dQPx/69ad7109-3f3d-489b-a31d-fc8ea760c2e7/lesson.html`, `CHANGELOG.md`).
- **2026-08-18**: Persisted the first prepare/render failure that triggers a bounded repair on the lesson as `first_error`, and emitted that diagnostic from JSON logs so a repaired generation can be diagnosed without reproducing the model output (`backend/app/core/logging.py`, `backend/app/lessons/generation.py`, `backend/app/db/models.py`, `backend/app/db/session.py`, `backend/app/api/schemas.py`, `frontend/src/lib/types/api.ts`, `backend/scripts/sync_lessons.py`, `backend/tests/test_v2_api.py`, `backend/tests/test_v2_app.py`, `CHANGELOG.md`).
- **2026-08-18**: Top-aligned the grid rows inside Slides comparison lists so short bullet sets retain normal reading flow within equal-height comparison cards (`backend/app/lessons/formats/slides/assets/v1/blocks/diagrams.css`, `backend/tests/test_v2_app.py`, `.env/storage/lessons/user_3G5KeDTZSDI5ogr34JnDy27dQPx/c72da3d5-0606-4649-afe1-9308df5b9bcb/lesson.html`, `CHANGELOG.md`).
- **2026-08-18**: Recovered every illegal JSON string control in one Slides parse pass—raw quotes, unknown or trailing backslashes, and control characters—so first-pass model output no longer needs a second repair just to become parseable (`backend/app/lessons/formats/slides/strategy.py`, `backend/tests/test_v2_app.py`, `CHANGELOG.md`).
- **2026-08-18**: Stopped routine Slides generations from always spending a second model pass on `Repairing the slide specification…` by locally recovering the first-pass JSON mistakes that usually fail validation: unescaped quotes in `custom-html`, trailing commas, extra keys, empty quiz fields on content slides, and a single body object instead of an array. Irrecoverable JSON still fails without repair, failed generations keep the rejected output, and retry logs now include the validation error (`backend/app/lessons/formats/slides/strategy.py`, `backend/app/lessons/formats/slides/prompt.py`, `backend/app/lessons/generation.py`, `frontend/src/lib/hooks/useGeneration.ts`, `backend/tests/test_v2_app.py`, `backend/tests/test_v2_api.py`, `CHANGELOG.md`).
- **2026-08-18**: Top-aligned ordinary Slides bullet grids and reduced their extra vertical gap to normal reading spacing while preserving the stronger separation between numbered solution steps; added compiler regression coverage and rebuilt the affected local DNA lesson artifact from its canonical specification (`backend/app/lessons/formats/slides/assets/v1/blocks/content.css`, `backend/tests/test_v2_app.py`, `.env/chalksmith.local.db`, `.env/storage/lessons/user_3G5KeDTZSDI5ogr34JnDy27dQPx/43dff2fc-88fe-41cb-8487-d62b1185ea36/lesson.html`, `CHANGELOG.md`).
- **2026-08-18**: Added a bounded `custom-html` escape hatch for Slides representations not covered by the semantic Block Catalog, with a five-slide lesson limit, one-Block-per-slide composition, learner-visible capacity accounting, allowlisted and scoped HTML/CSS/inline SVG, conditional styling and KaTeX assets, prompt guidance that preserves semantic Blocks as the default, security and compiler regression coverage, and updated architecture documentation (`backend/app/lessons/formats/slides/`, `backend/tests/test_v2_api.py`, `backend/tests/test_v2_app.py`, `backend/AGENTS.md`, `doc/LAYOUT.md`, `doc/DIAGRAMS.md`, `CHANGELOG.md`).
- **2026-08-17**: Added a fully local debugging mode for working without a usable network. `backend/app/integrations/storage.py` became a package behind a `Storage` protocol, with the existing GCS client in `gcp.py` and a new filesystem backend in `local.py` that maps object keys to paths under `LOCAL_STORAGE_DIR` and returns an API route where GCS returns a signed URL; `main.py` mounts that route only when the setting is present, it rejects keys resolving outside the directory, and `Settings` refuses the setting under `staging`/`production` because a deployed disk is ephemeral. `./bin/debug.sh start --local` exports the directory alongside a SQLite `DATABASE_URL`, skipping the Cloud SQL proxy and the bucket reachability check, while Clerk sign-in and Vertex are untouched; the local database is deliberately allowed to diverge from staging, which is disposable (`backend/app/integrations/storage/`, `backend/app/api/local_storage.py`, `backend/app/main.py`, `backend/app/core/config.py`, `backend/app/api/lessons.py`, `backend/app/api/generations.py`, `backend/app/lessons/generation.py`, `backend/tests/test_v2_app.py`, `bin/debug.sh`, `bin/env.local.template`, `backend/AGENTS.md`, `README.md`, `doc/GCP.md`, `CHANGELOG.md`).
- **2026-08-17**: Upgraded the existing Slides `geometry-model` from a generic outline into a semantic geometry renderer with triangle subtypes, named vertices and intersections, compiler-positioned construction segments, right-angle and congruence markings, shape-aware validation, collision-resistant labels, Prompt guidance, regression coverage, and full registered-Block browser QA so geometry lessons such as Pythagorean and Ceva diagrams render as meaningful figures instead of decorated text (`backend/app/lessons/formats/slides/blocks/math.py`, `backend/app/lessons/formats/slides/blocks/__init__.py`, `backend/app/lessons/formats/slides/assets/v1/blocks/math.css`, `backend/app/lessons/formats/slides/prompt.py`, `backend/tests/test_v2_app.py`, `doc/LAYOUT.md`, `doc/DIAGRAMS.md`, `CHANGELOG.md`).
- **2026-08-17**: Added backend contribution requirements to review the complete existing Slides Block library before adding or removing a Block, request user direction when capabilities overlap, and visually verify every registered Block during Slides testing (`backend/AGENTS.md`, `CHANGELOG.md`).
- **2026-08-17**: Split the Slides visual runtime into shared core CSS and domain-aligned Block style groups, taught the Registry to derive each Block's group, and made the compiler embed only the groups required by a lesson while loading KaTeX only for Equation Blocks; added regression coverage and updated the architecture guides so generated artifacts remain deterministic without carrying the entire visual library (`backend/app/lessons/formats/slides/registry.py`, `backend/app/lessons/formats/slides/compiler.py`, `backend/app/lessons/formats/slides/assets/v1/`, `backend/tests/test_v2_app.py`, `backend/AGENTS.md`, `doc/LAYOUT.md`, `doc/DIAGRAMS.md`, `CHANGELOG.md`).
- **2026-08-17**: Expanded the Slides visual vocabulary with eight cross-subject diagrams in `diagrams.py`—Venn, grouped cause/effect, ordered layers, many-to-many networks, qualitative quadrants, continuous spectra, nested containment, and categorical matrices—and separated discipline-owned function, force, wave, particle, reaction, and cell Blocks into `math.py`, `physics.py`, `chemistry.py`, and `biology.py`; added semantic compatibility and capacity validation, deterministic HTML/SVG/CSS rendering, model-selection guidance, compiler regressions, architecture documentation, and reusable generation/visual-QA topics (`backend/app/lessons/formats/slides/`, `backend/tests/test_v2_app.py`, `backend/AGENTS.md`, `doc/LAYOUT.md`, `doc/DIAGRAMS.md`, `CHANGELOG.md`).
- **2026-08-17**: Corrected the Slides CDN fallback probe to inspect Reveal's currently presented slide instead of always testing the first slide, preventing fast navigation during the one-second readiness window from incorrectly activating fallback mode and displacing later slides (`backend/app/lessons/formats/slides/compiler.py`, `backend/tests/test_v2_app.py`, `CHANGELOG.md`).
- **2026-08-17**: Expanded the structured Slides visual library with deterministic `pyramid-diagram`, `hierarchy-tree`, and branching `flow-diagram` Blocks, including bounded semantic Specs, model-facing guidance, collision-resistant HTML/CSS renderers, validation and escaping coverage, and explicit Prompt guidance against misusing a generic geometry triangle for layered or relational concepts (`backend/app/lessons/formats/slides/blocks/`, `backend/app/lessons/formats/slides/prompt.py`, `backend/app/lessons/formats/slides/assets/v1/runtime.css`, `backend/tests/test_v2_app.py`, `doc/LAYOUT.md`, `CHANGELOG.md`).
- **2026-08-17**: Enabled the source-code viewer's native long-line wrapping so generated HTML and Python remain readable within the lesson preview instead of requiring horizontal scrolling (`frontend/src/app/generation/page.tsx`, `CHANGELOG.md`).
- **2026-08-17**: Moved the Web, API, and renderer Dockerfiles from `infra/docker/` to the root `docker/` directory, removing an otherwise empty infrastructure layer and updating Cloud Build paths and repository structure documentation without changing Docker build contexts (`docker/`, `bin/cloudbuild-backend.yaml`, `bin/cloudbuild-web.yaml`, `README.md`, `backend/AGENTS.md`, `doc/REFACTOR.md`, `CHANGELOG.md`).
- **2026-08-17**: Moved Reveal.js and KaTeX versions, asset tags, math initialization, and CDN fallback ownership from the shared HTML Renderer into the deterministic Slides Compiler and runtime CSS. Removed arbitrary Reveal markup normalization and injection so `render/html.py` is now a format-neutral final security boundary for marker validation, blocked JavaScript APIs, nonterminating-loop detection, CSP injection, and artifact output; updated generation regression coverage and architecture documentation accordingly (`backend/app/lessons/formats/slides/compiler.py`, `backend/app/lessons/formats/slides/assets/v1/runtime.css`, `backend/app/lessons/render/html.py`, `backend/tests/test_v2_api.py`, `backend/AGENTS.md`, `doc/LAYOUT.md`, `CHANGELOG.md`).
- **2026-08-17**: Reorganized structured Slides Blocks as domain-oriented vertical slices: content, math/model, data, and diagram modules now colocate each Block's Pydantic contract, LLM guide, validation, and deterministic HTML renderer; a top-level Registry owns aggregation, Prompt Catalog formatting, capability sets, and render dispatch, while the lesson-level Spec stays independent and compact cross-Block layout rules remain with Reveal document compilation. Registry coverage tests and architecture documentation preserve the v1 Schema and rendered behavior as the Block vocabulary grows (`backend/app/lessons/formats/slides/`, `backend/tests/test_v2_app.py`, `backend/AGENTS.md`, `doc/LAYOUT.md`, `CHANGELOG.md`).
- **2026-08-17**: Top-aligned textual and symbolic Slides Block content—statement, bullets, callout, steps, equation, comparison, and process—while preserving centered visual canvases, so sparse cards begin at a consistent reading edge instead of floating vertically; Slides compilation now rereads the runtime stylesheet instead of retaining a process-lifetime copy, preventing subsequent local generations from embedding stale CSS after a style edit (`backend/app/lessons/formats/slides/assets/v1/runtime.css`, `backend/app/lessons/formats/slides/compiler.py`, `CHANGELOG.md`).
- **2026-08-17**: Expanded structured Slides from mostly textual cards into a documented visual vocabulary: added a single model-facing Block Catalog with rendered-behavior guidance and examples, soft visual-diversity and Slide Kind prompting, validated number-line, proportional bar, bar-chart, coordinate-plot, geometry, labeled-diagram, cycle, and timeline specifications, deterministic platform-owned renderers and runtime styles, schema/catalog/compiler regression coverage, and aligned layout/backend documentation while retaining `chalksmith.slides.v1` and v1.1 runtime/compiler identifiers for the disposable staging dataset (`backend/app/lessons/formats/slides/`, `backend/tests/test_v2_app.py`, `backend/AGENTS.md`, `doc/LAYOUT.md`, `CHANGELOG.md`).
- **2026-08-16**: Updated the backend agent guide to match the consolidated lesson-generation domain, using a nested directory tree for API, core, database, integrations, format slices, renderers, scripts, and tests; also refreshed Slides v1, prompt, environment-template, and new-format guidance so future changes follow the current boundaries (`backend/AGENTS.md`, `CHANGELOG.md`).
- **2026-08-16**: Reworked the in-development `chalksmith.slides.v1` contract around semantic slide kinds and typed blocks instead of model-selected course templates. The prompt now recommends—but does not enforce—a teaching sequence, validation owns capacity and incompatible complex-block combinations, the compiler deterministically selects single, split, thirds, solution, or visual layouts, obsolete template metadata was removed across persistence and API types, and disposable staging runtime/compiler metadata was normalized to v1.1 (`backend/app/lessons/formats/slides/`, `backend/app/lessons/formats/contracts.py`, `backend/app/lessons/generation.py`, `backend/app/db/`, `backend/app/api/schemas.py`, `backend/tests/`, `frontend/src/lib/types/api.ts`, `backend/AGENTS.md`, `doc/LAYOUT.md`, `CHANGELOG.md`).
- **2026-08-16**: Renamed the lesson artifact package from `backend/app/lessons/rendering/` to `backend/app/lessons/render/` for a shorter domain path, updating application imports, renderer test patch targets, migration tooling, and architecture documentation without changing rendering behavior (`backend/app/lessons/render/`, `backend/app/`, `backend/scripts/migrate_v1.py`, `backend/tests/`, `backend/AGENTS.md`, `doc/LAYOUT.md`, `CHANGELOG.md`).
- **2026-08-16**: Colocated generated-code response parsing with its only consumer, `CodeLessonStrategy`, and removed the now-empty `backend/app/lessons/parsing/` package. Interactive and Video retain the same summary separator, Markdown-fence cleanup, bounded Python trailing-text recovery, and HTML document truncation behavior, while tests and architecture documentation now point to `backend/app/lessons/formats/code.py` (`backend/app/lessons/formats/code.py`, `backend/app/lessons/parsing/`, `backend/tests/test_v2_app.py`, `backend/AGENTS.md`, `doc/LAYOUT.md`, `CHANGELOG.md`).
- **2026-08-16**: Consolidated the short lesson-format support modules around their reasons to change: shared request, result, strategy, and strict-spec contracts now live in `backend/app/lessons/formats/contracts.py`, while code-format prompt construction is colocated with `CodeLessonStrategy` in `backend/app/lessons/formats/code.py`; kept `registry.py` as the independent format-selection boundary and updated imports, tests, and architecture guidance (`backend/app/lessons/formats/`, `backend/tests/test_v2_api.py`, `backend/AGENTS.md`, `doc/LAYOUT.md`, `CHANGELOG.md`).
- **2026-08-16**: Removed the obsolete Legacy Slides Prompt and structured-Slides rollback flag. New Slides now always use the validated specification compiler, while historical Slides without `lesson_spec` remain readable but return an explicit `legacy_lesson_read_only` conflict before any edit reaches the model; deployment templates, architecture guidance, and regression coverage now enforce that boundary (`backend/app/lessons/formats/`, `backend/app/lessons/generation.py`, `backend/app/api/generations.py`, `backend/app/core/config.py`, `backend/tests/`, `bin/deploy.sh`, `bin/env.local.template`, `bin/env.deploy.template`, `backend/AGENTS.md`, `doc/LAYOUT.md`, `CHANGELOG.md`).
- **2026-08-16**: Consolidated lesson generation into the `backend/app/lessons/` domain package and removed the former `services/`, `renderers/`, and `lesson_runtime/` modules without compatibility wrappers. Format behavior is now organized as Slides, Interactive, and Video vertical slices with colocated prompts and versioned assets; shared generation orchestration, code parsing, source extraction, and rendering live beside them, and application imports, tests, migration tooling, and architecture documentation now reference the new boundary (`backend/app/lessons/`, `backend/app/api/`, `backend/app/renderer_main.py`, `backend/scripts/migrate_v1.py`, `backend/tests/`, `backend/AGENTS.md`, `README.md`, `doc/LAYOUT.md`, `doc/REFACTOR.md`, `CHANGELOG.md`).
- **2026-08-16**: Prevented Reveal.js from interpreting structured Slides body, comparison, and comprehension containers as nested vertical slides, which had absolutely positioned the body over the title. The compiler now reserves `<section>` for top-level lesson slides, emits Reveal's canonical `.slides` container, bumps to v1.2, and asserts the safe structure in API regression coverage (`backend/app/services/compilers/slides.py`, `backend/tests/test_v2_api.py`, `CHANGELOG.md`).
- **2026-08-16**: Fixed structured Slides rendering every Reveal section at once: the runtime's unconditional `display: grid !important` overrode Reveal's hidden state for past and future slides, stacking every absolutely positioned title and body in the same viewport. Non-current Slides are now explicitly hidden and only the direct `.present` section becomes a grid; bumped the embedded runtime/compiler to v1.1 and added compiled-artifact regression assertions (`backend/app/lesson_runtime/assets/v1/slides.css`, `backend/app/services/compilers/slides.py`, `backend/tests/test_v2_api.py`, `backend/tests/test_v2_app.py`, `CHANGELOG.md`).
- **2026-08-16**: Implemented the first format-native lesson pipeline for new Slides: providers now return a strict versioned JSON teaching specification, a deterministic compiler owns Reveal markup and embedded Chalkboard runtime styles, invalid specifications receive one bounded semantic repair, and edits use the prior canonical specification instead of generated HTML. Added schema/template/runtime/compiler persistence with an additive migration and rollback setting, preserved the legacy path for existing rows, constrained the frontend preview to a shared 16:9 viewport, added compiler/API/edit/migration/security fixtures and regression coverage, and updated deployment templates and architecture guidance (`backend/app/services/lesson_specs/`, `backend/app/services/compilers/`, `backend/app/services/lesson_formats.py`, `backend/app/lesson_runtime/`, `backend/app/services/generation.py`, `backend/app/db/`, `backend/app/api/`, `backend/app/core/config.py`, `backend/tests/`, `frontend/src/app/generation/page.tsx`, `frontend/src/components/generation/LessonViewport.tsx`, `frontend/src/lib/types/api.ts`, `bin/deploy.sh`, `bin/env.local.template`, `bin/env.deploy.template`, `backend/AGENTS.md`, `doc/LAYOUT.md`, `CHANGELOG.md`).
- **2026-08-16**: Defined a format-native lesson output architecture that replaces arbitrary model-authored pages and scenes with declarative Slides and Video specifications, a controlled Interactive shell with restricted p5 logic, deterministic versioned compilers, canonical 16:9 rendering, format-specific validation, bounded specification repair, compatibility metadata, rollout phases, and measurable acceptance criteria so layout quality is enforced by construction rather than CSS patching (`doc/LAYOUT.md`, `CHANGELOG.md`).
- **2026-08-15**: Stopped `setup.sh` from minting a second database password over the working one. Its `gcloud secrets describe` probe ran with the output discarded, which also swallowed gcloud's periodic reauth prompt, so a `ReauthRequiredError` read as "the secret does not exist"; the run then tried to create a duplicate secret and, because the check ran as an `if` condition where `set -e` is disabled, carried on past the resulting 409 and generated a fresh password anyway. Reads now go through `gc_probe`, which counts only a real not-found as absent and aborts on anything else, and reauth is forced up front where its prompt is visible; the same silent probe was replaced for the bucket, Cloud SQL instance, database, and user checks, each of which could have created a resource that already existed. Staging was reconciled to the password version that run had written, confirmed by connecting as the Cloud SQL user with both versions (`bin/setup.sh`, `CHANGELOG.md`).
- **2026-08-15**: Aligned the generation page's input box with the bottom edge of the lesson-preview card, which sat higher because its panel was sized by its content rather than filled; the panel now fills its height and carries the preview section's own `pb-8` offset, and the textarea grows with it instead of holding a fixed height. The character counter and the icon buttons cancel their hit-area padding so their visual edges land on the container's inset rather than six pixels inside it, the topic limit rises from 100 to 200 characters within the 500 the backend already accepted, and the per-generation status line was dropped because the chat history's own indicator already reports it (`frontend/src/components/generation/InputForm.tsx`, `frontend/src/components/generation/GenerationSidebar.tsx`, `frontend/src/components/ui/Textarea.tsx`, `CHANGELOG.md`).
- **2026-08-15**: Repaired the Moon-phases interactive lesson whose four descending p5.js loops incremented forever and froze the preview, updating both its production database source and GCS HTML. Added HTML validation that rejects counter updates moving away from their stopping bound before saving, prompt guidance that prevents the mistake, a format-neutral automatic-repair prompt, and regression coverage so generated interactives are repaired instead of reaching browsers with an obvious infinite loop (`backend/app/renderers/html.py`, `backend/app/services/prompts.py`, `backend/tests/test_v2_api.py`, production lesson `f2349944-0be9-4c00-a615-1ae613894a6a`, `CHANGELOG.md`).
- **2026-08-15**: Added `sync_lessons.py` so staging can be refreshed from production repeatedly rather than by a one-off copy. Each environment authenticates against its own Clerk instance and `object_key` embeds the owner, so every row and artifact is rewritten for the target's account; owners are matched on `external_id` where an import recorded one and on verified email otherwise, which keeps the mapping valid in either direction. Rows are upserted and artifacts are re-copied only when the source MD5 differs, so a synced pair reports no work; deletions are the one destructive step and stay behind `--prune` (`backend/scripts/sync_lessons.py`, `CHANGELOG.md`).
- **2026-08-15**: Rejected generated HTML whose counter loop updates away from its stopping condition, because such a loop never terminates and freezes the preview iframe rather than failing visibly. String and comment contents are masked before the scan so lesson text quoting a loop is not mistaken for one, and `<script src=...>` blocks are skipped; the interactive prompt states the rule so the model avoids it in the first place (`backend/app/renderers/html.py`, `backend/tests/test_v2_api.py`).
- **2026-08-15**: Stopped video lessons from failing as unparseable code. `parse_generated_lesson` treated everything after `---CODE_START---` as code and only stripped a leading Markdown fence, so anything the model appended became part of the source; reproducing the prompt against `gemini-3.7-flash` showed it mirroring the opening separator with an invented `---CODE_END---`, which made `ast.parse` reject the scene and the repair retry reproduce the same shape. Rather than stripping that one literal, the parser now lets the code's own grammar decide where it ends — HTML truncates after `</html>`, Python keeps the longest prefix that parses — bounded to `MAX_TRAILING_LINES` so a genuinely malformed response still reaches validation instead of being silently truncated into a shorter valid program. The prompt also now states that the last line of code ends the response. Failed generations additionally persist the rejected code, which this bug could only be diagnosed without by replaying the prompt (`backend/app/services/prompts.py`, `backend/app/services/generation.py`, `backend/tests/test_v2_app.py`, `CHANGELOG.md`).
- **2026-08-15**: Restored the two v1 Manim lessons that never rendered at all: their generated code ends in `FadeOut(VGroup(*self.mobjects))`, which raises because `self.mobjects` holds plain `Mobject` instances, so the render died after the partial clips and the MP4 was never assembled — the reason these two of twelve had no artifact to migrate. Switched that one line to `Group`, re-rendered at the 720p30 the other v1 videos use, and stored the corrected source alongside so the lesson can be regenerated from what it actually shows (`CHANGELOG.md`).
- **2026-08-14**: Made `setup.sh` rotate the Clerk secret instead of only seeding it: `needs_value` skipped any secret that already held a version, so switching Clerk applications required a manual `gcloud secrets versions add`. A key file is now authoritative and `sync_secret_value` adds a version only when the live value differs, leaving superseded versions enabled to roll back to; the generated database password deliberately keeps the old create-once path so it cannot desync from its Cloud SQL user. Added `migrate_v1_users.py` and rebuilt the 20 v1 accounts in the production Clerk instance: Clerk cannot move users between instances, so v1's Development ids could not be kept, and each import instead records its old id in `external_id` and joins through the v1 email to emit the owner map. Reading users from a Dashboard CSV export rather than the Backend API is what carries the 9 bcrypt password digests over, since the API never returns them; the other 11 accounts are OAuth and relink on their verified email. Lesson ownership was then remapped in place rather than re-migrated: `object_key` is only ever stored and read back verbatim, so 90 rows changed owner without re-uploading a single artifact, and the 88 objects were afterwards rewritten server-side onto `lessons/<new owner>/…` in copy-verify-repoint-delete order so no lesson was ever without a reachable artifact (`bin/setup.sh`, `backend/scripts/migrate_v1_users.py`, `CHANGELOG.md`).
- **2026-08-14**: Migrated the 90 v1 lessons into production, and fixed the three defects that made `migrate_v1.py` unable to complete: its `INSERT` omitted the `NOT NULL` `root_lesson_id`/`version_number` columns and would have aborted on the first row, it sourced every artifact from the v1 `static/` tree even though v1 gitignored that directory and committed only 39 of the 90 referenced files, and it passed v1's naive timestamps into `timestamptz` columns. Artifacts for slides and interactive lessons are now rebuilt from the `lesson.code` document each row already stores — verified byte-identical to all 29 committed HTML files — through the same normalization the v2 renderer applies, so only video still needs a legacy file; that raised the migration from 39 to 88 recovered artifacts, leaving 2 Manim lessons whose `.mp4` was never committed as `failed` with their source retained for re-rendering (`backend/scripts/migrate_v1.py`, `CHANGELOG.md`).
- **2026-08-14**: Stopped DeepSeek lessons from failing as malformed output by disabling its default high-effort thinking mode, which bills the chain of thought against `LLM_MAX_OUTPUT_TOKENS` and returns it in a `reasoning_content` field this backend never reads: a measured lesson prompt spent all 65536 output tokens on reasoning and produced zero content in 562 seconds. Added `DEEPSEEK_THINKING` for the opt-in, reported a length-truncated answer as an exhausted token budget rather than a missing code separator, and covered both behaviors with tests (`backend/app/integrations/llm/deepseek.py`, `backend/app/core/config.py`, `backend/app/integrations/llm/factory.py`, `backend/tests/test_v2_app.py`, `bin/deploy.sh`, `bin/env.local.template`, `bin/env.deploy.template`, `doc/GCP.md`, `AGENTS.md`).
- **2026-08-14**: Added native Vertex Gemini response streaming with accumulated-character SSE progress, moving progress-bar feedback, ten-second heartbeats for non-streaming providers, full-response buffering before validation/rendering, single completion-level usage logging, deadline/cancellation preservation, and regression coverage and architecture/cost documentation (`backend/app/integrations/llm/`, `backend/app/services/generation.py`, `backend/tests/`, `frontend/src/lib/`, `README.md`, `doc/COST.md`, `backend/AGENTS.md`, `AGENTS.md`).
- **2026-08-14**: Increased the source-material input ceiling from 120,000 to 200,000 characters, balancing larger PDF contexts against Gemini input spend, latency, and compatibility with smaller provider context windows, and documented the approximate English/CJK token-cost range (`backend/app/core/config.py`, `bin/deploy.sh`, `bin/env.local.template`, `bin/env.deploy.template`, `doc/COST.md`, `AGENTS.md`).
- **2026-08-14**: Reduced lesson-version database and frontend preview work with aggregate numbering, lightweight history responses, batch updates, cached signed URLs, and shared format/error handling; cleaned partial generation objects on failure, exposed LLM token/source deployment guardrails, expired Cloud Build source archives, pinned Cloud Run to request-based CPU billing, and reduced duplicate API logs (`backend/app/`, `backend/tests/`, `frontend/src/`, `bin/deploy.sh`, `bin/env.deploy.template`, `bin/cloudbuild-source-lifecycle.json`, `infra/docker/api.Dockerfile`, `doc/COST.md`, `doc/GCP.md`, `AGENTS.md`).
- **2026-08-14**: Added DeepSeek as a third LLM provider with its own OpenAI-compatible `/chat/completions` client, because the existing OpenAI provider's Responses API call is not served by DeepSeek, reading `DEEPSEEK_API_KEY` and an overridable `DEEPSEEK_BASE_URL`, requiring only the selected provider's key in production, mounting `LLM_SECRET_NAME` under the provider's own variable name during deployment, and documenting the mutually exclusive local and deployment template blocks (`backend/app/integrations/llm/deepseek.py`, `backend/app/integrations/llm/factory.py`, `backend/app/core/config.py`, `backend/tests/test_v2_app.py`, `bin/deploy.sh`, `bin/env.local.template`, `bin/env.deploy.template`, `doc/GCP.md`, `AGENTS.md`).
- **2026-08-14**: Replaced the ignored deployment wrapper requirement with a strict, literal `.env/env.deploy` loader in `deploy.sh` where exported variables retain precedence, added a documented deployment template covering required Vertex/domain values, the OpenAI Secret Manager alternative, staging origins, and optional resource overrides, migrated this workstation's non-secret wrapper values into the ignored deployment file, and updated deployment, domain, and Clerk instructions (`bin/deploy.sh`, `bin/env.deploy.template`, `.env/env.deploy`, `README.md`, `doc/GCP.md`, `doc/DOMAIN.md`, `doc/CLERK.md`, `AGENTS.md`).
- **2026-08-14**: Added a copyable local-runtime environment template documenting the shared web/API configuration, CORS-to-Clerk authorized-party fallback, local service base URLs, ADC-based Vertex defaults, mutually exclusive OpenAI settings, Cloud SQL Proxy URL, staging GCS signer, optional backend tuning values, and secure copy workflow, and made it the documented source for `.env/env.local` (`bin/env.local.template`, `doc/GCP.md`, `README.md`, `AGENTS.md`).
- **2026-08-14**: Removed redundant `CLERK_AUTHORIZED_PARTIES` entries from the ignored staging and production Clerk files, added a copyable Clerk key template that distinguishes Development/staging `pk_test_`/`sk_test_` values from Production `pk_live_`/`sk_live_` values and documents optional issuer overrides, and aligned authentication and GCP guidance with the existing `FRONTEND_ORIGINS` fallback and production domain-derived injection (`.env/clerk.key.stg`, `.env/clerk.key.prod`, `bin/clerk.key.template`, `doc/CLERK.md`, `doc/GCP.md`, `AGENTS.md`).
- **2026-08-14**: Replaced the ambiguous Clerk application-URL instruction with executable Development and Production Dashboard flows, clarified root-domain DNS/certificate setup, subdomain sessions, environment-specific keys and issuers, production secret provisioning and rotation, and corrected deploy/domain documentation so `run.app` is treated as a diagnostic URL rather than a production Clerk allowlist target (`doc/CLERK.md`, `bin/deploy.sh`, `README.md`, `doc/GCP.md`, `doc/DOMAIN.md`, `AGENTS.md`).
- **2026-08-14**: Added a dedicated custom-domain operations guide covering the current deploy boundary, direct Cloud Run mappings, DNS and managed certificates, root/`www`/`app` host setup, Clerk production DNS, canonical redirects versus satellite domains for a second base domain, verification, troubleshooting, and lifecycle commands, and linked it from the deployment documentation (`doc/DOMAIN.md`, `doc/GCP.md`, `README.md`, `AGENTS.md`).
- **2026-08-14**: Simplified production deployment to `DOMAIN=example.com ./bin/deploy.sh prod start`, deriving root/`www`/`app` origins inside the script and compiling the same domain into Next.js host routing and metadata while retaining domain-optional staging deployments (`bin/deploy.sh`, `bin/cloudbuild-web.yaml`, `infra/docker/web.Dockerfile`, `frontend/src/proxy.ts`, `frontend/src/app/layout.tsx`, `README.md`, `doc/GCP.md`, `AGENTS.md`).
- **2026-08-14**: Aligned the Dashboard `Lessons` heading baseline with the Sidebar Logo and increased its spacing from the lesson-card grid without materially moving the cards (`frontend/src/app/dashboard/page.tsx`, `AGENTS.md`).
- **2026-08-14**: Updated the site-header navigation to show `Dashboard`, `Explore`, and `About Us` in that order, retaining the existing `/content` route behind the renamed Explore link and using Next.js links for all three destinations (`frontend/src/app/layout.tsx`, `AGENTS.md`).
- **2026-08-14**: Replaced the Dashboard Logo's hard-coded production-domain link with a current-origin relative homepage route so local, staging, custom-domain, and production deployments navigate correctly (`frontend/src/components/dashboard/DashboardSidebar.tsx`, `AGENTS.md`).
- **2026-08-14**: Made all visible dashboard lesson-card content click through to the card's lesson link while preserving the top-right actions button, dropdown, and deletion-state behavior (`frontend/src/components/dashboard/LessonCard.tsx`, `AGENTS.md`).
- **2026-08-14**: Increased version-label contrast in the generation chat by using the primary white text color on amber user-message cards while retaining secondary text on assistant cards (`frontend/src/components/generation/GenerationSidebar.tsx`, `AGENTS.md`).
- **2026-08-14**: Kept version selection anchored to the selected conversation instead of scrolling chat history to the bottom, retained bottom-following only for active generation, and added a persistent `Version X` badge to the lesson preview and code view (`frontend/src/app/generation/page.tsx`, `frontend/src/components/generation/GenerationSidebar.tsx`, `AGENTS.md`).
- **2026-08-14**: Removed the white flash between saved-lesson metadata loading and media rendering by keeping the loading overlay visible until the iframe or video reports usable content and by replacing the iframe's white backing surface with the chalkboard background (`frontend/src/app/generation/page.tsx`, `AGENTS.md`).
- **2026-08-13**: Added an accessible indeterminate progress animation when opening saved lessons while retaining determinate stage progress during generation, extracted the loading overlay into a reusable component, and added a reduced-motion fallback (`frontend/src/components/generation/LoadingOverlay.tsx`, `frontend/src/app/generation/page.tsx`, `frontend/src/lib/hooks/useGeneration.ts`, `frontend/src/app/globals.css`, `AGENTS.md`).
- **2026-08-13**: Prevented generated Reveal.js presentations from becoming a featureless white preview when their CDN runtime is unavailable by adding an inline dark-page baseline, a first-slide recovery fallback, matching generation guidance, and renderer regression coverage; repaired and audited both saved presentations without changing their lesson IDs (`backend/app/renderers/html.py`, `backend/app/services/prompts.py`, `backend/tests/test_v2_api.py`, GCS lesson objects, `AGENTS.md`).
- **2026-08-13**: Fixed saved lesson previews stalling under the two-connection Cloud SQL pool by closing ordinary request sessions before responses, preventing committed generation objects and signed-URL work from retaining connections, moving mixed database/storage lesson handlers off the event loop, cancelling duplicate frontend loads, and showing actionable preview errors; documented and tested the session-lifecycle invariant (`backend/app/`, `backend/tests/`, `backend/AGENTS.md`, `frontend/src/`, `doc/GCP.md`, `AGENTS.md`).
- **2026-08-13**: Made Cloud SQL start and stop operations resilient to long-running updates by submitting activation-policy patches asynchronously and explicitly waiting without the shorter `gcloud` synchronous timeout; documented the lifecycle wait behavior (`bin/setup.sh`, `doc/GCP.md`, `AGENTS.md`).
- **2026-08-12**: Added a backend-scoped agent context file documenting the API/renderer split, the generation SSE pipeline, lesson versioning, tenant-isolation and sandboxing invariants, configuration and migration constraints, and the unittest conventions, so future backend work starts from the real code rather than rediscovering it (`backend/AGENTS.md`, `AGENTS.md`).
- **2026-08-12**: Removed the unused database lesson-deletion helper, replaced dashboard version-count N+1 queries with one owner-scoped grouped query and regression coverage, consolidated dashboard layout/list fetching/rendering into shared components and a hook, and reduced homepage delivery size with a real favicon plus resized logo and optimized demo videos while retaining the legal Markdown archives (`backend/app/db/lessons.py`, `backend/app/api/lessons.py`, `backend/tests/test_v2_api.py`, `frontend/src/app/dashboard/`, `frontend/src/components/dashboard/`, `frontend/src/lib/hooks/useLessons.ts`, `frontend/public/favicon.ico`, `frontend/public/logo.png`, `frontend/public/demo-1.mp4`, `frontend/public/demo-3.mp4`, `frontend/public/demo-4.mp4`, `AGENTS.md`).
- **2026-08-12**: Fixed responsive p5.js pointer guidance so CSS-scaled canvases use p5's logical `mouseX`/`mouseY` coordinates without a second scale conversion, added regression coverage, and repaired the saved matrix-multiplication interactive and its private GCS preview without changing its lesson ID (`backend/app/services/prompts.py`, `backend/tests/test_v2_api.py`, `.env/chalksmith.local.db`, GCS lesson object).
- **2026-08-12**: Switched the default Vertex AI model to the verified GA `gemini-3.6-flash` across local configuration, environment templates, and deployment documentation (`.env/.env.backend.local`, `backend/.env.example`, `GCP.md`, `README.md`).
- **2026-08-12**: Moved the production deployment command from `GCP.md` to `README.md`, retaining GCP prerequisites, runtime identities, secrets, database initialization/v1 migration, and troubleshooting in the GCP guide (`README.md`, `GCP.md`).
- **2026-08-12**: Aligned backend and frontend environment examples with the centralized local environment files and GCP local-development settings, including the local credential path, Vertex model, development GCS bucket, and signing service account (`backend/.env.example`, `frontend/.env.example`).
- **2026-08-12**: Removed the redundant `infra/gcloud/README.md`; `GCP.md` is the single source of truth for Google Cloud setup and production deployment guidance (`infra/gcloud/README.md`, `GCP.md`).
- **2026-08-12**: Renumbered the `GCP.md` production deployment section and all internal references from 4.x to 2.x (`GCP.md`).
- **2026-08-12**: Reformatted the `GCP.md` Project prerequisites IAM matrix to show required role, purpose, API, and Local/Production applicability in the requested table layout (`GCP.md`).
- **2026-08-12**: Consolidated local-debug and production-deployment IAM prerequisites and API enablement into Project prerequisites tables in `GCP.md`; removed the duplicate production role and API guidance from Section 4.1 (`GCP.md`).
- **2026-08-11**: Consolidated all production deployment guidance under Section 4 of `GCP.md`, including deployment permissions and APIs, Cloud Run identities, secrets, deployment, database migration, and production troubleshooting; clarified that the Section 2 service account and JSON key are local-development-only (`GCP.md`).
- **2026-08-11**: Grouped lesson edits into persistent version histories with root/parent/version metadata, a version-history API, dashboard root-only listing and version counts, selectable generation-chat revisions, and safe whole-history rename/delete behavior; linked the existing local “请展示勾股定理” records into their four preserved versions and recovered its legacy edit instructions from their generated summaries where the former implementation had not stored the original user text (`backend/app/db/`, `backend/app/api/`, `backend/app/services/generation.py`, `backend/tests/test_v2_api.py`, `frontend/src/`, `.env/chalksmith.local.db`).
- **2026-08-11**: Prevented saved lesson previews from rendering iframe or video elements with an empty `src` while their signed GCS URL is loading, eliminating recursive page requests and the related React console error (`frontend/src/app/generation/page.tsx`).
- **2026-08-11**: Prevented blank Reveal.js lesson previews by pinning generated slides to verified Reveal.js 4.5.0 assets, normalizing or injecting those assets during HTML rendering, adding regression coverage, and repairing the existing Ceva's Theorem lesson in the local database and private GCS object without changing its ID or teaching content (`backend/app/services/prompts.py`, `backend/app/renderers/html.py`, `backend/tests/test_v2_api.py`, `.env/chalksmith.local.db`, GCS lesson object).
- **2026-08-11**: Linked the existing Next.js frontend to the ChalkSmithGCP Clerk application through the Clerk CLI, scaffolded hosted sign-in and sign-up routes, updated the Clerk auto-proxy matcher for Next.js 16, and centralized the frontend Clerk configuration under the ignored root environment directory (`frontend/src/app/sign-in/`, `frontend/src/app/sign-up/`, `frontend/src/proxy.ts`, `.env/.env.frontend.local`).
- **2026-08-11**: Restored Clerk authentication while retaining the v2 direct browser-to-FastAPI boundary: replaced custom browser auth and backend admin-token verification with Clerk UI/session JWTs and cached JWKS verification, kept JWT `sub` tenant ownership, removed obsolete auth dependencies/configuration, added a least-privilege web deployment identity and secret injection, rewrote architecture/cloud/auth documentation and legal provider disclosures, preserved v1 Clerk IDs in migration guidance, and updated local ignored environment templates (`frontend/src/`, `frontend/package.json`, `frontend/package-lock.json`, `backend/app/`, `backend/pyproject.toml`, `backend/uv.lock`, `backend/tests/`, `backend/scripts/migrate_v1.py`, `infra/`, `.env/`, `CLERK.md`, `GCP.md`, `README.md`, `REFACTOR.md`, `frontend/public/`, `AGENTS.md`).
- **2026-08-11**: Consolidated all Google Cloud prerequisites and operations into a single-source `GCP.md`, including Vertex AI service-account ADC, private development GCS provisioning, centralized local environment files, organization-policy and IAM troubleshooting, Cloud SQL, and Cloud Run deployment; linked it from `README.md` while preserving provider-independent local run/test instructions (`GCP.md`, `README.md`).
- **2026-08-11**: Centralized all local runtime configuration, SQLite data, and Google service-account credentials under the ignored root `.env/` directory; taught Next.js and FastAPI to load their dedicated centralized files, resolved relative ADC paths safely, configured the actual Vertex/GCS service identity, excluded `.env/` from Docker contexts, tightened local file permissions, updated tests and documentation, and verified service-account token refresh plus Vertex model access (`.gitignore`, `.dockerignore`, `.env/`, `frontend/next.config.ts`, `backend/.env.example`, `backend/app/core/config.py`, `backend/app/db/session.py`, `backend/tests/test_v2_app.py`, `README.md`, `REFACTOR.md`).
- **2026-08-11**: Migrated the default Gemini integration from the Developer API key flow to Vertex AI with ADC/service-account authentication, added project/location validation and impersonated-ADC signed-URL reuse, updated Cloud Run IAM and conditional OpenAI secret handling, configured local Vertex settings, expanded tests, and aligned architecture, local-debug, deployment, and refactor documentation (`backend/.env.example`, `backend/.env.local`, `backend/app/core/config.py`, `backend/app/integrations/llm/`, `backend/app/integrations/storage.py`, `backend/tests/test_v2_app.py`, `infra/gcloud/deploy.sh`, `infra/gcloud/README.md`, `README.md`, `REFACTOR.md`).
- **2026-08-11**: Normalized the private GCS bucket styling in the README system architecture diagram so it matches the other components (`README.md`).
- **2026-08-11**: Documented the administrator and developer workflow for provisioning a separate private local-debug GCS bucket, applying lifecycle and public-access protections, granting least-privilege object and signed-URL impersonation roles, configuring backend storage variables, and initializing local ADC (`README.md`).
- **2026-08-11**: Upgraded the frontend to Next.js 16.3.0 and a patched PostCSS dependency range, refreshed transitive Nano ID and Sharp packages to remove four high-severity audit findings, and reviewed unnecessary dependency install scripts so clean installs have no unreviewed lifecycle scripts (`frontend/package.json`, `frontend/package-lock.json`).
- **2026-08-11**: Reconciled `REFACTOR.md` with the implemented v2 system by correcting the POST access-URL flow, HTML/video renderer split, API/CRUD responsibilities, configuration list, current directory structure, phase and test status, GCP verification wording, and launch criteria; enforced Uniform Bucket-Level Access plus Public Access Prevention in the GCS deployment path and aligned operational documentation and the `v1.0` branch reference (`REFACTOR.md`, `infra/gcloud/deploy.sh`, `infra/gcloud/README.md`, `README.md`).
- **2026-08-11**: Rewrote `README.md` architecture, local debugging, and Google Cloud deployment documentation to match the implemented three-service system, direct authenticated browser-to-FastAPI flow, isolated Manim renderer, independent frontend/backend runtimes, local ADC/GCS requirements, deployment automation, migration safeguards, and production verification steps while preserving the project introduction, examples, motivation, contact, and author content (`README.md`).
- **2026-08-11**: Standardized frontend filenames by aligning the `Textarea` component file with its export, normalizing public assets and legal Markdown documents to lowercase kebab-case, updating all runtime references, and aligning the `FormatOutput` function name with its file (`frontend/public/`, `frontend/src/app/`, `frontend/src/components/`).
- **2026-08-10**: Completed the post-refactor audit outside the deferred Gemini audience-policy question: hardened isolated Manim validation and cancellation, end-to-end generation deadlines, source limits, request/error logging, storage/delete consistency, sandboxing, authentication credential linking, and prompts; extracted generation state to `frontend/src/lib/hooks/useGeneration.ts`; removed four unused frontend modules and redundant dependency/config entries; simplified `.gitignore`/`.dockerignore`; preserved `frontend/public/Privacy-Policy.md` and `frontend/public/Terms-of-Service.md`; expanded the backend suite to 22 passing tests and reverified strict frontend type-check and production build (`backend/app/`, `backend/scripts/`, `backend/tests/`, `frontend/src/`, dependency manifests, `infra/`, `.gitignore`, `.dockerignore`, `README.md`, `REFACTOR.md`).
- **2026-08-10**: Removed the obsolete daemontools/VPS backend launcher; Docker and GCP deployment remain the supported service startup paths (`scripts/server/chalksmith-backend`).
- **2026-08-10**: Removed obsolete Git ignore entries for retired Vercel/Yarn/PnP workflows, unused frontend output/test directories, and the v1 `backend/static/` artifact directory; retained only artifacts the current npm, Next.js, and FastAPI workflows can produce (`.gitignore`).
- **2026-08-10**: Removed the redundant `frontend/.npmrc`, whose optional-dependency settings match npm defaults and caused a warning during checks; consolidated duplicate environment, cache, and dependency ignore rules without changing their coverage (`frontend/.npmrc`, `.gitignore`).
- **2026-08-10**: Removed the single-workspace root npm layer and made `frontend/` an independent Node.js project by localizing its lockfile and dependency tree, updating Next.js standalone configuration, Docker build paths, development documentation, and the refactor structure (`package.json`, `package-lock.json`, `frontend/package.json`, `frontend/package-lock.json`, `frontend/next.config.ts`, `infra/docker/web.Dockerfile`, `README.md`, `REFACTOR.md`, `.gitignore`).
- **2026-08-10**: Completed the Chalksmith v2 refactor across `backend/app/`, `backend/scripts/`, `backend/pyproject.toml`, `frontend/src/`, package manifests, `infra/`, `README.md`, and `REFACTOR.md`: replaced the v1 backend with tenant-isolated FastAPI/SQLModel APIs, private GCS storage, PDF extraction, configurable Gemini/OpenAI adapters, CSP-protected HTML and an isolated Manim renderer; removed Next.js lesson API proxies while retaining direct Clerk Bearer-token API calls; removed Remotion, v1 services, daemontools, stale architecture art, and tracked local generated outputs; added dry-run data migration, Cloud Run/Cloud SQL deployment assets, production configuration validation, request logging, twelve backend tests, and successful frontend type-check/build verification.
- **2026-08-10**: Configured uv-based Python environment management for the v2 backend with `backend/pyproject.toml`, `backend/.python-version`, `backend/uv.lock`, and a CPython 3.12 `backend/.venv`; updated `REFACTOR.md` commands, marked `backend/requirements.txt` as temporary v1 compatibility, and verified all four v2 backend tests plus the FastAPI application import.
- **2026-08-10**: Started Phase 1 of the v2 refactor by adding the parallel `backend/app/` FastAPI entrypoint with centralized validated configuration, JSON logging, shared errors, CORS, and the readiness endpoint; added `backend/tests/test_v2_app.py`, new frontend API/auth/type boundaries under `frontend/src/lib/`, expanded `backend/.env.example`, and recorded verified GCP authentication and remaining deployment-permission requirements in `REFACTOR.md` while preserving all v1 runtime paths.
- **2026-08-10**: Added and refined `REFACTOR.md` with a simplified v2 architecture plan covering the frontend/backend structure, Python FastAPI modules, Clerk authentication, direct authenticated API flow, a configurable LLM Provider contract with Vertex AI Gemini and OpenAI adapters, GCP services, Mermaid diagrams, migration phases, security boundaries, testing, and completion criteria.
- **2026-07-03**: Fixed source-backed lesson generation receiving `405 Method Not Allowed` by moving the static `/lesson/generate` GET/POST routes above dynamic `/lesson/{lesson_id}` routes in `backend/routers/lesson.py` so `generate` is not interpreted as a lesson id.
- **2026-07-03**: Fixed generation auth error handling by preserving streamed upstream status in `frontend/src/lib/api.ts` and updating `frontend/src/app/generation/page.tsx` so Unauthorized/session failures show a session-required modal with no Auto-Fix path instead of being described as invalid generated lessons.
- **2026-07-03**: Added a source upload validation message in `backend/routers/lesson.py` so PDFs that extract zero characters stream a user-facing `"FILE_NAME" has no encrypted text layer.` error instead of continuing generation silently.
- **2026-07-03**: Added terminal debug output for uploaded source extraction in `backend/routers/lesson.py` so PDF filenames, extracted character counts, and extracted text print when source-backed generation runs.
- **2026-07-03**: Wired PDF source uploads into lesson generation by passing selected files from `frontend/src/components/ui/TextArea.tsx` through `frontend/src/components/generation/InputForm.tsx`, `frontend/src/components/generation/GenerationSidebar.tsx`, and `frontend/src/app/generation/page.tsx`, adding multipart streaming support in `frontend/src/lib/api.ts` and `frontend/src/app/api/lesson-generate/route.ts`, supporting combined source contexts in `backend/routers/lesson.py`, and tightening source extraction/proxy handling in `backend/services/sources.py` and `frontend/src/app/api/sources/upload/route.ts`.
- **2026-07-01**: Matched the dashboard search input and format filter styling by updating `frontend/src/app/dashboard/search/page.tsx` and `frontend/src/components/dashboard/SearchFilter.tsx` so the controls share the same background, height, and corner radius.
- **2026-07-01**: Refactored the dashboard lesson format dropdown into a custom `frontend/src/components/dashboard/SearchFilter.tsx` component while keeping the text search bar markup and search state in `frontend/src/app/dashboard/search/page.tsx`.
- **2026-07-01**: Deleted the unused dashboard favorites page at `frontend/src/app/dashboard/favorites/page.tsx` and built a dashboard lesson search view in `frontend/src/app/dashboard/search/page.tsx` with a debounced search input, shared dropdown format filter, loading/error/empty states, and reusable lesson cards.
- **2026-07-01**: Cleaned up saved lesson search wiring by making `backend/crud/lessons.py` handle optional query and format filters, updating `backend/routers/lesson.py` to use the shared search path, forwarding query parameters through `frontend/src/app/api/lesson-list/route.ts`, normalizing `frontend/src/lib/api.ts`, and fixing the dashboard search link in `frontend/src/components/dashboard/DashboardSidebar.tsx`.
- **2026-06-26**: Added missing frontend dependencies for Clerk localization and markdown/math rendering in `frontend/package.json` and refreshed the workspace `package-lock.json` so `frontend/src/app/layout.tsx` and `frontend/src/components/ui/FormatOutput.tsx` resolve during Next.js builds.
- **2026-06-26**: Added a homepage FAQ dropdown section in `frontend/src/components/home/FaqSection.tsx` and rendered it after the examples carousel in `frontend/src/app/page.tsx` to answer common teacher questions before the final CTA.
- **2026-06-25**: Connected temporary PDF source uploads to lesson generation by replacing `backend/services/sources.py` with a LangChain/PyMuPDF extractor, adding multipart POST streaming generation in `backend/routers/lesson.py`, passing source context through `backend/services/llm.py`, fixing the source preview route in `backend/routers/sources.py`, and adding source-upload dependencies to `backend/requirements.txt`.
- **2026-06-25**: Tightened the physics p5.js sidebar in `frontend/public/ex_interactive.html` by reducing control spacing, widening the sidebar, shortening the instruction text, and compacting the formula copy so bottom-left text stays inside the scaled 16:9 shell.
- **2026-06-25**: Applied the same internal responsive scaling approach to the physics p5.js lab in `frontend/public/ex_interactive.html`, fitting the sidebar and canvas to a 16:9 shell and remapping pointer coordinates so dragging still works after scaling.
- **2026-06-25**: Moved p5.js example scaling from the iframe shell into `frontend/public/ex_interactive.html` by fitting the 900x600 sketch to the carousel iframe at runtime and scaling the p5-created controls with the same layout; restored the carousel iframe in `frontend/src/components/home/ExamplesCarousel.tsx` to normal sizing.
- **2026-06-25**: Scaled only the p5.js example preview in `frontend/src/components/home/ExamplesCarousel.tsx` by rendering the interactive iframe larger and applying a 0.8 transform inside the shared 16:9 frame.
- **2026-06-25**: Updated `frontend/src/components/home/ExamplesCarousel.tsx` so video, p5.js interactive, and Reveal.js slide examples all use the same 16:9 media frame ratio.
- **2026-06-25**: Scaled down the drawn clock and timeline icons in the new elapsed-time p5.js example at `frontend/public/ex_interactive.html`, including the clock face, tick marks, hands, center pin, timeline nodes, active tracker, and arrowheads.
- **2026-06-25**: Scaled down the p5.js example sidebar and controls in `frontend/public/ex_interactive.html`, including narrower sidebar sizing, smaller slider thumbs, tighter buttons/stats, and a smaller draggable laser source.
- **2026-06-25**: Reapplied proportional scaling to the restored original p5.js interactive in `frontend/public/ex_interactive.html`, reducing the ray length, arcs, labels, ray widths, pulse dots, and draggable laser source while preserving the restored control styling.
- **2026-06-25**: Scaled down the p5.js optical scene in `frontend/public/ex_interactive.html` by reducing ray length, arc size, label size, ray stroke widths, pulse markers, and the draggable laser source so the interactive has more breathing room in the examples carousel.
- **2026-06-25**: Tightened the carousel example assets in `frontend/public/ex_interactive.html` and `frontend/public/ex_slides.html`, and adjusted `frontend/src/components/home/ExamplesCarousel.tsx` so p5.js controls, canvas content, and Reveal.js slides fit inside the homepage examples frame.
- **2026-06-25**: Added an examples carousel to the homepage with `frontend/src/components/home/ExamplesCarousel.tsx`, wired it into `frontend/src/app/page.tsx`, and added `frontend/public/example_reveal.html` so examples can show Manim video, p5.js, and Reveal.js formats.
- **2026-06-25**: Adjusted the connect section alignment in `frontend/src/app/about/page.tsx` so the contact text sits higher while staying visually centered with the books image.
- **2026-06-25**: Fixed the about page Substack contact icon in `frontend/src/app/about/page.tsx` by switching from an unloaded Bootstrap Icons class to a Font Awesome icon, and removed the invalid jsDelivr stylesheet link from `frontend/src/app/layout.tsx`.
- **2026-06-25**: Reworked the about page contact section in `frontend/src/app/about/page.tsx` into a left-side social contact list with the book image on the right, and restored the landing page footer on the about page.
- **2026-06-25**: Enlarged the about page portrait in `frontend/src/app/about/page.tsx` and made its desktop frame stretch to match the height of the adjacent founder text.
- **2026-06-25**: Rebuilt the about page in `frontend/src/app/about/page.tsx` with valid JSX styling, a polished chalkboard-themed founder layout, and Font Awesome icon usage loaded from `frontend/src/app/layout.tsx`.
- **2026-06-20**: Constrained `frontend/src/components/ui/Modal.tsx` to a viewport-safe maximum height with an internal scroll area, and tightened the generation error modal in `frontend/src/app/generation/page.tsx` so long error/source text scrolls without overflowing off-screen.
- **2026-06-20**: Moved `/api/lesson-generate` authentication into `frontend/src/app/api/lesson-generate/route.ts` and removed it from the protected lesson API proxy matcher in `frontend/src/proxy.ts` so EventSource generation failures return SSE error payloads instead of Clerk middleware 404 HTML responses.
- **2026-06-20**: Updated `deploy/daemontools/chalksmith-backend/run` so the VPS backend service binds to `0.0.0.0` for direct network exposure instead of loopback-only access.
- **2026-06-20**: Added daemontools deployment support for the FastAPI backend with `deploy/daemontools/chalksmith-backend/run`, `deploy/daemontools/chalksmith-backend/log/run`, and `scripts/server/chalksmith-backend`, guarded macOS-only `pyobjc` requirements, and updated the NumPy pin in `backend/requirements.txt` so Ubuntu Python 3.12 installs can complete.
- **2026-06-19**: Added a diagram-backed bullet-point architecture section in `README.md` that embeds `Architecture.png` and explains how the Next.js frontend, FastAPI backend, Neon database, Clerk/Svix auth flow, LLM providers, and rendering pipelines work together.
- **2026-06-14**: Updated `frontend/src/app/api/lesson-generate/route.ts` so generation proxy failures are returned as SSE error messages instead of JSON responses that EventSource reports only as a generic connection error, and recorded the change in `AGENTS.md`.
- **2026-06-13**: Added a shared auth redirect target in `frontend/src/lib/auth-redirects.ts`, wired marketing sign-up buttons in `frontend/src/app/page.tsx`, `frontend/src/app/layout.tsx`, and `frontend/src/components/home/ProfileLink.tsx` to send completed sign-up/sign-in flows directly to the app generation page, updated the dashboard create-new link in `frontend/src/app/dashboard/page.tsx`, restored `frontend/.env.example` with Clerk multi-domain notes, and allowed env example files in `.gitignore`.
- **2026-06-13**: Fixed saved lesson dashboard navigation crashing in `frontend/src/app/generation/page.tsx` by removing an invalid named `React` import, using named React hooks consistently, and guarding title rename handling until a loaded lesson result exists.
- **2026-06-13**: Added backend CRUD modules in `backend/crud/lessons.py` and `backend/crud/users.py`, moved lesson and user SQLModel database reads/writes out of `backend/routers/lesson.py` and `backend/routers/users.py`, and kept tenant-isolated lesson fetching in `get_user_lessons`.
- **2026-06-13**: Moved Clerk protection for lesson API routes into `frontend/src/proxy.ts`, added the trusted proxy auth header helper in `frontend/src/lib/auth-headers.ts`, and removed duplicated route-level `auth()` calls from `frontend/src/app/api/lesson-generate/route.ts`, `frontend/src/app/api/lesson-record/route.ts`, `frontend/src/app/api/lesson-list/route.ts`, `frontend/src/app/api/lessons/route.ts`, and `frontend/src/app/api/lesson-export/route.ts` so lesson handlers rely on proxy-verified identity without opening unauthenticated access.
- **2026-06-13**: Updated the homepage interaction cue hover styling in `frontend/src/components/home/CodeDrivenDemo.tsx` so it transitions into the same amber treatment as the material toggle.
- **2026-06-13**: Added a small interaction cue to `frontend/src/components/home/CodeDrivenDemo.tsx` so homepage visitors know they can try the embedded preview controls.
- **2026-06-13**: Added a strict Reveal.js screen containment rule in `backend/services/llm.py` so generated presentations keep all slide elements within the visible viewport instead of clipping or overflowing.
- **2026-06-13**: Fixed lesson generation finalization crashing with `name 'User' is not defined` by importing `User` in `backend/routers/lesson.py` before checking or lazily creating authenticated users.
- **2026-06-13**: Fixed missing icon assets by adding `frontend/public/favicon.ico` and pointing the Apple icon metadata in `frontend/src/app/layout.tsx` at `frontend/public/logo.png`.
- **2026-06-13**: Hardened Clerk webhook user syncing by updating `frontend/src/app/api/webhooks/clerk/route.ts` to verify the raw Svix request body, select Clerk's primary email address, and fail clearly when backend webhook env vars are missing; updated `backend/routers/users.py` to avoid logging internal secrets and report missing webhook auth configuration.
- **2026-06-12**: Fixed Clerk `UserButton` dark styling in `frontend/src/app/layout.tsx` by importing the `dark` theme object from `@clerk/themes` instead of passing the ignored string value, and removed the unused `ProfileLink` import.
- **2026-06-12**: Fixed signed-in users being redirected to sign-in from marketing app links by reordering `frontend/src/proxy.ts` so `chalksmith.ai/generation` and `chalksmith.ai/dashboard` redirect to `app.chalksmith.ai` before Clerk route protection runs.
- **2026-06-12**: Fixed the homepage code-driven demo by updating `frontend/src/components/home/CodeDrivenDemo.tsx` to reuse the generation page's code toggle appearance, show a title/description header, extract the embedded p5.js source for matching syntax colors, keep code overflow inside the demo frame, and updated `frontend/src/app/page.tsx` to render the demo.
- **2026-06-11**: Removed all subscription, billing, and Stripe-related code, database fields, and legal documentation. Retired usage tracking and quotas. Removed `ModelSelector` from the UI and set `gemini-3.5-flash` as the default model to simplify the generation flow.
- **2026-06-11**: Updated logo usage in `frontend/src/app/layout.tsx`, `frontend/src/app/page.tsx`, `frontend/src/app/generation/page.tsx`, and `frontend/src/components/dashboard/DashboardSidebar.tsx` to import the bundled asset from `frontend/src/content/logo.png` instead of referencing `/logo.png`.
- **2026-06-11**: Moved the generation input character counter inside the bottom-left of `frontend/src/components/generation/InputForm.tsx` and added bottom padding so the counter and submit/stop icon remain contained within the textarea without covering input text.
- **2026-06-11**: Added a 100-character topic/edit input limit in `frontend/src/components/generation/InputForm.tsx` with a visible character counter so generation prompts stay concise before submission.
- **2026-06-11**: Stopped hidden Remotion generation from fresh lessons by changing `frontend/src/app/generation/page.tsx` to default to the visible `manim` video format, aligning success/loading copy with the shown format options, and removing the stale Remotion option comment from `frontend/src/components/generation/FormatSelector.tsx`.
- **2026-06-11**: Reduced Remotion render memory pressure in `backend/services/render.py` by sanitizing generated scene props, lowering default render dimensions/FPS/concurrency, redirecting Remotion CLI logs to disk instead of memory, and tightened `backend/services/llm.py` Remotion prompts to keep generated videos within a smaller render budget.
- **2026-06-11**: Installed Vercel AI SDK streaming support by adding `ai` and `@ai-sdk/react` to `frontend/package.json` and creating `frontend/src/app/api/chat/route.ts`, an authenticated AI Gateway streaming route for Chalksmith STEM assistant responses.
- **2026-06-11**: Rebuilt the legal pages in `frontend/src/app/privacy-policy/page.tsx` and `frontend/src/app/terms-of-service/page.tsx` with matching dark layouts, all-white text styling, and table-of-contents links so the policy and terms pages share the same format without uninstalled markdown dependencies.
- **2026-06-10**: Finished lesson ownership enforcement by requiring authenticated user headers across `backend/routers/lesson.py`, scoping lesson fetch/edit/delete/list/export queries by `Lesson.user_id`, saving `user_id` on new generated lessons, forwarding Clerk user headers from `frontend/src/app/api/lesson-record/route.ts`, `frontend/src/app/api/lesson-list/route.ts`, and `frontend/src/app/api/lessons/route.ts`, and adding a `lesson.user_id` compatibility column check in `backend/database.py`.
- **2026-06-10**: Wired authenticated monthly lesson usage enforcement into the real generation flow by adding `backend/services/usage.py`, extending `backend/models.py` with `User.usage_month`, registering Neon-backed models in `backend/database.py`, applying quota checks in `backend/routers/lesson.py` and `backend/main.py`, forwarding Clerk user headers from `frontend/src/app/api/lesson-generate/route.ts` and `frontend/src/app/api/lesson-record/route.ts`, and adding the psycopg Postgres driver in `backend/requirements.txt`; new lessons count toward usage while edits with `lesson_id` do not.
- **2026-06-10**: Reworked `frontend/src/components/home/FireParticleBackground.tsx` into a Three.js interpretation of the golden-amber murmuration prompt: particles originate from the bottom-left, spiral across the hero, taper toward the top-right, and vary between sharp dense core lines and soft bokeh outer mist.
- **2026-06-10**: Deleted `frontend/src/components/home/ParticleWaveBackground.tsx` and ported the earlier canvas-based amber wave from commit `17c3510` into a Three.js `FireParticleBackground` using one `THREE.Points` buffer, GPU sine/cosine wave motion, visible amber sprite particles, and pointer repulsion; updated `frontend/src/app/page.tsx` to use it.
- **2026-06-10**: Removed a redundant incompatible Clerk `authObj.protect()` call from `frontend/src/proxy.ts` so the frontend build can type-check against the installed Clerk middleware API while preserving the existing `await auth.protect()` route protection.
- **2026-06-10**: Replaced the first homepage Three.js background with `frontend/src/components/home/ParticleWaveBackground.tsx`, a fresh visible amber particle wave that uses a single `THREE.Points` shader system and parts around the user pointer; updated `frontend/src/app/page.tsx` to render the new component.
- **2026-06-10**: Tuned the homepage Three.js particle shader in `frontend/src/components/home/MurmurationBackground.tsx` and adjusted the hero overlay in `frontend/src/app/page.tsx` so the landing page reads more like a dense amber school of fish moving in diagonal wave ribbons.
- **2026-06-10**: Added a shader-driven Three.js murmuration background in `frontend/src/components/home/MurmurationBackground.tsx`, installed `three` and `@types/three`, and updated `frontend/src/app/page.tsx` to render the optimized amber particle field behind the homepage hero.
- **2026-06-09**: Fixed the backend CORS defaults in `backend/main.py` so local development and Chalksmith production domains are allowed even when `FRONTEND_ORIGINS` is not set, while still allowing deployment-specific origins from the environment.
- **2026-06-09**: Added a frontend build-time Clerk environment check in `frontend/next.config.ts` and documented required deployment variables in `frontend/.env.example` so Vercel fails clearly when `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` is missing.
- **2026-06-09**: Fixed Render backend startup in `backend/main.py` by importing dotenv and defining the CORS origin regex, and added `beautifulsoup4` to `backend/requirements.txt` so the Manim docs startup helper has its explicit dependency in clean deploy builds.
- **2026-06-09**: Enhanced backend error logging in `backend/routers/content.py` and added a startup write-permission check for the `static/` directory in `backend/main.py` to diagnose post-deployment "Internal Server Error" issues.
- **2026-06-09**: Removed server-side Clerk `Show` usage from `frontend/src/app/layout.tsx` and `frontend/src/app/page.tsx`, moving signed-in UI checks to client-side Clerk hooks in `frontend/src/components/layout/ProfileLink.tsx` so the marketing homepage no longer triggers server auth middleware errors.
- **2026-06-09**: Removed the root layout server-side Clerk user lookup in `frontend/src/app/layout.tsx` and added `frontend/src/components/layout/ProfileLink.tsx` so the header avatar renders client-side without triggering `auth()` middleware detection errors.
- **2026-06-09**: Moved Clerk route protection and app subdomain routing into `frontend/src/proxy.ts` and removed `frontend/src/middleware.ts` so Next 16 uses a single proxy entrypoint for `app.chalksmith.ai` rewrites.
- **2026-06-09**: Updated `frontend/src/app/layout.tsx`, `frontend/src/app/globals.css`, `frontend/src/app/dashboard/page.tsx`, and `frontend/src/app/generation/page.tsx` so the root header sticks to the top, is hidden on app workspace pages, and links the signed-in profile image to `/dashboard`.
- **2026-06-09**: Linked the homepage "Build a lesson now" CTAs in `frontend/src/app/page.tsx` to Clerk sign-in for signed-out users while keeping signed-in users routed to `/generation`.
- **2026-06-09**: Hardened deployment API connectivity by normalizing backend CORS origins in `backend/main.py`, adding the same-origin streaming proxy `frontend/src/app/api/lesson-generate/route.ts`, and updating frontend API calls in `frontend/src/lib/api.ts`, `frontend/src/app/api/lesson-record/route.ts`, `frontend/src/app/api/lesson-list/route.ts`, and `frontend/src/app/api/lessons/route.ts` so lesson generation no longer depends on browser cross-origin SSE.
- **2026-06-09**: Reduced homepage particle background cost in `frontend/src/components/home/FireParticleBackground.tsx` by lowering particle count, capping animation to 24fps, using 1x canvas resolution, and removing blur/shadow filters.
- **2026-06-09**: Reworked the homepage particle background in `frontend/src/components/home/FireParticleBackground.tsx` into a coordinated golden-amber murmuration flow with sharp core particles and blurred outer mist.
- **2026-06-09**: Replaced the homepage fire particle emitter with a slow-moving amber particle wave in `frontend/src/components/home/FireParticleBackground.tsx` for a calmer chalkboard background effect.
- **2026-06-08**: Center-aligned the plus and equals operators with the Educator's Dilemma stat boxes in `frontend/src/app/page.tsx`.
- **2026-06-08**: Grouped the homepage footer Legal and Contact columns together on the right in `frontend/src/app/page.tsx` and restored the final CTA section closing wrappers.
- **2026-06-08**: Widened homepage footer column spacing in `frontend/src/app/page.tsx` so the Legal links sit farther from the Chalksmith copyright block.
- **2026-06-08**: Increased bottom spacing before the homepage footer in `frontend/src/app/page.tsx` so the final CTA has more visual breathing room.
- **2026-06-08**: Moved the homepage feature card JSX directly into the "Why choose Chalksmith?" section of `frontend/src/app/page.tsx` and removed the local `FeatureMockup` helper.
- **2026-06-08**: Inlined the homepage feature mockup component into `frontend/src/app/page.tsx` and deleted `frontend/src/components/home/HomepageMockups.tsx` to keep homepage-specific code in the route file.
- **2026-06-08**: Rebuilt the homepage from the Figma design in `frontend/src/app/page.tsx`, added `frontend/src/components/home/FireParticleBackground.tsx` for an interactive mouse-reactive fire particle canvas, and added `frontend/src/components/home/HomepageMockups.tsx` for product preview and feature panels with legal/footer links.
- **2026-06-08**: Added user-friendly LLM region/access error handling in `backend/services/llm.py` and updated frontend SSE handling in `frontend/src/lib/api.ts` and `frontend/src/app/generation/page.tsx` so blocked country/region provider errors show a clear message to users.
- **2026-06-08**: Added `.npmrc` and `frontend/.npmrc` to force npm to include optional native dependencies during deployment so packages like Lightning CSS install their Linux binaries on Vercel.
- **2026-06-08**: Fixed case-sensitive frontend import in `frontend/src/components/generation/InputForm.tsx` so the deployed build resolves the tracked `frontend/src/components/ui/Textarea.tsx` component on Linux/Vercel.
- **2026-05-12**: Connected dashboard lesson listing. Updated `backend/models.py` and `backend/routers/content.py` to return lesson metadata, added `frontend/src/app/api/lessons/route.ts` as the Next.js proxy, and updated `frontend/src/lib/api.ts`, `frontend/src/app/dashboard/page.tsx`, and `frontend/src/components/dashboard/LessonCard.tsx` so saved lessons render and can be deleted from the dashboard.
- **2026-05-12**: Added valid placeholder route components in `frontend/src/app/favorites/page.tsx` and `frontend/src/app/search/page.tsx` so the frontend build can type-check routes linked from the dashboard sidebar.
- **2026-05-12**: Tightened TypeScript narrowing in `frontend/src/app/generation/page.tsx` by storing completed lesson responses in a local constant before updating chat state.
- **2026-05-12**: Fixed `frontend/src/components/dashboard/LessonCard.tsx` header alignment so the ellipsis action stays on the right and normalized saved lesson format labels for dashboard cards.
- **2026-05-12**: Increased saved lesson title size in `frontend/src/components/dashboard/LessonCard.tsx` for stronger dashboard card hierarchy.
- **2026-05-12**: Scoped the lesson card navigation overlay in `frontend/src/components/dashboard/LessonCard.tsx` to each card by making the card positioned and layering controls above the link.
- **2026-05-12**: Anchored the saved lesson date/model row and trash action to the bottom of `frontend/src/components/dashboard/LessonCard.tsx` cards for more consistent card alignment.
- **2026-05-14**: Added a responsive ellipsis action dropdown in `frontend/src/components/dashboard/LessonCard.tsx` with open and delete buttons so card actions are grouped behind the menu.
- **2026-05-14**: Raised the lesson card action header layer in `frontend/src/components/dashboard/LessonCard.tsx` so the ellipsis dropdown renders above the description text.
- **2026-05-14**: Fixed the dashboard rename modal in `frontend/src/components/dashboard/LessonCard.tsx` by adding local title state for `EditableTitle` instead of referencing an undefined setter.
- **2026-05-14**: Tightened the rename modal footer spacing in `frontend/src/components/dashboard/LessonCard.tsx` so the close button sits closer to the modal bottom.
- **2026-05-18**: Completed the lesson rename pathway by adding `LessonRenameRequest` in `backend/models.py`, updating `backend/routers/content.py` to read rename JSON bodies, and wiring `frontend/src/components/dashboard/LessonCard.tsx` to call `renameLesson` with the lesson id and handle rename errors.
- **2026-05-18**: Renamed frontend proxy folders to `lesson-record` and `lesson-list`, removed the empty `lesson_by_id` proxy stub, fixed the single-lesson proxy GET to use id-based backend loading, and updated `frontend/src/app/generation/page.tsx` to hydrate lessons opened from dashboard card links.
- **2026-05-07**: Moved `DashboardSidebar.tsx` to `components/dashboard/` and `GenerationSidebar.tsx` to `components/generation/`. Updated all import statements and relative paths accordingly. Reduced header vertical spacing and padding in sidebars for a more compact layout. Added `GET /content/lessons` endpoint to backend and improved dashboard lesson fetching.
- **2026-04-30**: Refined the Manim self-correction loop in `backend/services/render.py` by implementing automated traceback extraction for cleaner LLM prompts and increasing the retry limit to 2. Updated `backend/services/llm.py` with a dedicated "Self-Correction Mode" prompt that reinforces the original topic to prevent drift during auto-fixes. Enhanced the Dashboard UI in `frontend/src/app/dashboard/page.tsx` with smooth transitions and hover states for the "Create New Lesson" card.
- **2026-04-28**: Implemented robust mid-generation cancellation. Added `generationCleanupRef` and `stopGeneration` to the frontend to abort active SSE connections. Refactored backend rendering services to be asynchronous, using `asyncio.create_subprocess_exec` to allow immediate termination of long-running Manim and Remotion processes upon client disconnection. Updated the generation endpoint to monitor connection state and cancel active tasks accordingly.
- **2026-04-27**: Fixed "Objects are not valid as a React child" runtime error by preventing React event objects from being passed to `generateLesson` and adding a defensive type check in `frontend/src/app/generation/page.tsx` and `frontend/src/components/generation/InputForm.tsx`.
- **2026-04-27**: Resolved "No such file or directory: 'manim'" by fixing the executable path resolution in `backend/services/render.py` to correctly locate the binary within the virtual environment.
- **2026-04-27**: Fixed `SyntaxError` in Manim self-correction loop by refactoring `backend/services/llm.py` to ensure `generate_lesson` and `get_edit_prompt` return a dictionary and improving code extraction logic to strip natural language summaries.
- **2026-04-27**: Migrated `backend/services/export.py` to use `playwright.async_api` and updated `backend/routers/content.py` to `await` the export process, resolving a conflict with the FastAPI asyncio loop.
- **2026-04-27**: Added convenience scripts (`dev`, `frontend`, `backend`) to the root `package.json` to simplify multi-package development workflows. Recovered from a "Failed to load chunk" frontend error by clearing the `.next` build cache.
- **2026-04-23**: Standardized sidebar and interaction panel UI across the application. Implemented `InteractionPanel.tsx` in `frontend/src/components/generation/` to mirror the `DashboardSidebar.tsx` logic. Synchronized header heights (h-[40px]), padding (p-4), and vertical spacing (mb-8) to ensure toggle icons (PanelLeft/PanelRight) remain perfectly stationary during collapse/expand transitions in both the Dashboard and Generation pages.
- **2026-04-21**: Implemented lesson deletion functionality. Added `DELETE /content/lesson/{lesson_id}` endpoint in the backend to remove database records and associated physical files from `static/`. Updated the frontend API layer and server-side proxy to support deletion, and integrated it into the "Clear All" reset workflow in `frontend/src/app/generation/page.tsx`.
- **2026-04-17**: Implemented real-time lesson generation progress tracking using Server-Sent Events (SSE). Added a `GET /content/lesson/generate` streaming endpoint in the backend and integrated `EventSource` in the frontend. Replaced the loading spinner with a detailed 4-stage progress bar (Planning, Generating, Rendering, Finishing) in `frontend/src/app/generation/page.tsx`. Added progress callbacks to all rendering services in `backend/services/render.py` for granular status updates.
- **2026-04-16**: Resolved TypeScript module declaration error by installing `@types/react-katex`. Fixed a type mismatch in `frontend/src/remotion/Root.tsx` by making `scenes` optional in `RemotionVideoProps`.
- **2026-04-16**: Fixed critical "Internal Server Error" in Manim generation by recreating the broken Python virtual environment (`.venv`) and repairing local executable paths.
- **2026-04-16**: Implemented a "Self-Correction Loop" in `backend/services/render.py` that captures Manim Tracebacks and feeds them back to the LLM for automatic debugging and retries.
- **2026-04-16**: Automated Manim Community documentation synchronization. Created `backend/services/fetch_docs.py` to scrape `docs.manim.community` and integrated it into the FastAPI `lifespan` event to ensure the LLM always has access to the latest v0.18+ API reference, reducing code hallucinations.
- **2026-04-16**: Automated the "Auto-Fix" functionality to trigger AI generation directly from the error modal without modifying the user's input field. Expanded the error modal's code preview to display the full source code without truncation, improving the debugging experience for malformed blueprints.
- **2026-04-16**: Fixed TypeScript error "Property 'summary' does not exist on type 'LessonResponse'" by updating the interface in `frontend/src/lib/api.ts` to include `summary` and making `id` required to match the backend model.
- **2026-04-16**: Re-initialized the SQLite database by deleting `backend/lessons.db` to resolve a schema mismatch where the `summary` column was missing in the existing table.
- **2026-04-16**: Fixed a critical bug in `backend/database.py` where models were not being registered with `SQLModel.metadata` because they weren't imported before `create_all()`. Added an explicit import of `Lesson` and debug prints for database URL and registered tables.
- **2026-04-16**: Verified the `lesson` table schema using `sqlite3` and confirmed the `summary` column exists. Successfully restarted the backend (uvicorn) ensuring the `lifespan` event correctly initializes the database on startup. Cleaned up temporary backend debug log files.
- **2026-04-15**: Migrated the video generation engine from Manim to Remotion + KaTeX. Updated `llm.py` with a structured JSON prompt and `render.py` to handle `npx remotion render` calls. Implemented a frame-deterministic rendering engine in `RemotionVideo.tsx` using Remotion's native spring physics and `react-katex`. Integrated `@remotion/player` into the frontend for instant live previews. Updated `export.py` and the API router to support MP4 downloads and JSON blueprint fallbacks. Removed obsolete Manim compatibility layers and styling.
- **2026-04-12**: Fixed Python 3.9 compatibility issues in `backend/models.py` by replacing `|` with `Optional`. Resolved Gemini API SSL connection errors by removing redundant and problematic proxy manual environment setting in `backend/services/llm.py`. Fully implemented frontend "Edit Mode" in `frontend/src/app/generation/page.tsx` with `initialTopic` persistence and a "New Lesson" reset feature. Updated `InputForm.tsx` with dynamic mode-aware placeholders and disabled the `FormatSelector` during edits. Added `outline` variant and `size` support to `Button.tsx` to resolve TypeScript assignment errors. Updated `Dropdown.tsx` with refined `disabled` state styling (`text-stone-600`, `opacity-50`). Installed all backend dependencies (FastAPI, SQLModel, LLM SDKs, Manim, Playwright, etc.) and froze them into `backend/requirements.txt`.
- **2026-04-12**: Recovered project state following a git reset. Re-established NPM workspace structure by creating a root `package.json`. Reinstalled missing frontend dependencies (`react@19`, `react-dom@19`, `react-resizable-panels`, `lucide-react`) to fix broken builds. Restored Tailwind v4 styling by recreating `frontend/postcss.config.mjs`. Recreated the `Modal.tsx` component in `frontend/src/components/ui/` to restore the reset confirmation functionality.
- **2026-04-10**: Implemented an iterative editing feature across the backend and frontend. Updated `backend/models.py`, `backend/services/llm.py`, and `backend/routers/content.py` to support lesson context and specific edit prompts. Outlined the frontend state management strategy in `page.tsx` to handle "New" vs. "Edit" modes using a `currentLessonId` state.
- **2026-03-23**: Integrated `react-syntax-highlighter` into the "View Code" function in `frontend/src/app/generation/page.tsx`. Added dynamic language support for Manim (Python), p5.js (JavaScript), and Reveal.js (HTML) using the `vscDarkPlus` Prism theme. Replaced sparkle emojis (✨) in the chat history welcome message with `Flame` icons from `lucide-react` styled with the `text-accent` color.
- **2026-03-23**: Audited backend export strategies; confirmed `render_manim_lesson`, `render_p5js_lesson`, and `render_revealjs_lesson` correctly save assets to the `static/` directory and return valid absolute URLs. Removed the redundant and unused `backend/services/exporters/` directory to streamline the codebase.
- **2026-03-23**: Implemented a scalable `ExportService` in `backend/services/export.py` using a strategy pattern. Added a new `/content/export` GET endpoint in the backend and updated the `LessonResponse` model to include a unique lesson ID. Enhanced the frontend UI by adding an "Export" button in `frontend/src/app/generation/page.tsx`, allowing users to download generated lesson assets.
- **2026-03-23**: Fixed `PDFExportStrategy` in `backend/services/export.py` by making the export process asynchronous and correctly implementing the Playwright PDF conversion logic. Installed Chromium for Playwright on the server. Updated `backend/routers/content.py` to await the export preparation.
- **2026-03-23**: Refined export strategies: `reveal.js` presentations are now converted to PDF format using `playwright` for high-quality downloads, while `p5.js` remains as an interactive HTML export. Added `playwright` to `backend/requirements.txt`.
- **2026-03-17**: Enhanced `InputForm.tsx` by embedding the generate button (CircleArrowUp icon) in the bottom-right of the textarea and implemented a robust disabled state. Added a "View Code/Material" toggle with relative positioning and a custom regex-based syntax highlighter in `frontend/src/app/generation/page.tsx`. Fixed background color issues in the content area for consistent scrolling.
- **2026-03-14**: Configured NPM workspaces in the root `package.json` to resolve cross-package dependency issues and VS Code resolution errors. Stabilized the interaction panel collapse logic using percentage-based thresholds in `onResize` to prevent layout leaking during window resizing. Fixed critical syntax and indentation errors in `page.tsx` and integrated `framer-motion` (installed) for future UI enhancements.
- **2026-03-13**: Debugged and stabilized the resizable panel layout in `frontend/src/app/generation/page.tsx`. Corrected `react-resizable-panels` exports and fixed component nesting. Implemented a "narrow rail" collapse feature for the right interaction panel using the `collapsedSize` prop and imperative `panelRef` API.
- **2026-03-12**: Fixed Tailwind v4 rendering by renaming `postcss.config.ts` to `postcss.config.mjs` and correcting the theme block in `globals.css`. Relocated `public/` folder to the frontend root for Next.js compatibility. Implemented a 70/30 split UI with integrated chat history and preview state in `frontend/src/app/generation/page.tsx`, and reverted the root `page.tsx` to a placeholder. Refined `EditableTitle.tsx` for seamless header integration.
- **2026-03-08**: Completed `EditableTitle.tsx` and implemented `Card.tsx` component. Refactored `frontend/src/app/generation/page.tsx` into a Client Component with a 70/30 split layout, integrating dynamic content rendering for Manim (video) and p5.js/reveal.js (iframe). Updated `globals.css` with corrected Tailwind v4 theme variables.
- **2026-03-06**: Reorganized API architecture: migrated client-side utilities to `frontend/src/lib/api.ts`, implemented a server-side proxy in `frontend/src/app/api/lesson/route.ts`, and updated the frontend to use relative internal API paths. Removed redundant `frontend/src/app/api/route.ts`.
- **2026-03-04**: Fixed syntax and type errors in `InputForm.tsx`, updated `Button.tsx` with loading states and standard event props, and implemented the backend communication layer in `api.ts`. Reverted `page.tsx` to its original state per user request.
- **2026-03-02**: Debugged and updated `frontend/src/components/generation/ModelSelector.tsx`: integrated TypeScript types, aligned model selection with backend `llm.py` support (including Gemini, GPT-4o, DeepSeek, and Ark), and applied modular component architecture. Clarified roles of `Layout` vs. `Page` components for orchestration.
- **2026-03-01**: Implemented `Dropdown.tsx` in `frontend/src/components/ui/` featuring state management, a click-outside listener, and a conditional "amber theme" triggered upon value selection. The component includes a disabled state and smooth animations.
- **2026-03-01**: Summarized existing UI components in `frontend/src/components/ui/`: `Button.tsx` (variant-based styles), `Dropdown.tsx` (stateful selection), and `Textarea.tsx` (chalkboard-themed input).
- **2026-02-23**: Updated AGENTS.md with design system constants and logging requirements.
